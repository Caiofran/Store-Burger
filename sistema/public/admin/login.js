'use strict';
(async()=>{Brasa.hydrate();const form=document.querySelector('#login-form'),error=document.querySelector('#login-error');try{await API.init()}catch(e){error.textContent=e.message}form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button');btn.disabled=true;error.textContent='';try{await API.request('auth/login',Object.fromEntries(new FormData(form)));if(API.user.role==='customer'){await API.request('auth/logout',{});throw new Error('Use uma conta da equipe.')}location.replace('/admin/')}catch(e){error.textContent=e.message}finally{btn.disabled=false}})})();
